import { AwsInstrumentation } from '@opentelemetry/instrumentation-aws-sdk';
import { defineIntegration, SEMANTIC_ATTRIBUTE_SENTRY_ORIGIN } from '@sentry/core';
import { addOpenTelemetryInstrumentation } from '@sentry/node';

const _awsIntegration = (() => {
  return {
    name: 'Aws',
    setupOnce() {
      addOpenTelemetryInstrumentation(
        new AwsInstrumentation({
          preRequestHook(span) {
            span.setAttribute(SEMANTIC_ATTRIBUTE_SENTRY_ORIGIN, 'auto.otel.aws');
          },
        }),
      );
    },
  };
}) ;

/**
 * Instrumentation for aws-sdk package
 */
const awsIntegration = defineIntegration(_awsIntegration);

export { awsIntegration };
//# sourceMappingURL=aws.js.map
